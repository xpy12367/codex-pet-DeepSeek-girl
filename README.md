# DeepSeek娘 Codex Pet

这是一个 Codex Pet v2 包。

## 运行文件

- `pet.json`：宠物元数据，`spriteVersionNumber: 2`
- `spritesheet.webp`：1536×2288、8×11 精灵图

## 预览

`previews/` 包含标准动作联系表、16 方向观察图和 9 个标准状态 GIF。

## QA

`qa/` 包含 v2 尺寸验证、色键边缘清理、逐帧检查、方向语义、三人盲测和连续性结果。

本包目前仅交付在项目目录，未安装到 Codex。需要安装时，将本目录复制到 `%USERPROFILE%\.codex\pets\deepseek`。
